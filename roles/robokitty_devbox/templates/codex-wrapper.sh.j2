#!/bin/bash
set -euo pipefail

unset TELEGRAM_BOT_TOKEN
unset BOT_TOKEN
unset GH_TOKEN
unset GITHUB_TOKEN
unset OPENAI_API_KEY
unset CODEX_API_KEY
unset SSH_AUTH_SOCK

exec /usr/bin/sudo -H -n -u {{ robokitty_runner_user }} -- {{ robokitty_codex_binary | quote }} "$@"
